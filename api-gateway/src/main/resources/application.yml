spring:
  application:
    name: api-gateway

  cloud:
    gateway:
      default-filters:
        - DedupeResponseHeader=Access-Control-Allow-Origin Access-Control-Allow-Credentials, RETAIN_FIRST

      globalcors:
        add-to-simple-url-handler-mapping: true
        cors-configurations:
          '[/**]':
            allowedOrigins:
              # Env-driven so Render/Vercel can set the origin without redeploying code.
              - "${CORS_ALLOWED_ORIGINS:https://git-insight-ai-one.vercel.app}"
            allowedMethods:
              - GET
              - POST
              - PUT
              - DELETE
              - PATCH
              - OPTIONS
            allowedHeaders:
              - "*"
            allowCredentials: true
            maxAge: 3600

      routes:
        # ── Auth Service ──────────────────────────────────────────────
        # Direct env-var URLs for Render (each service is its own host).
        # lb://AUTH-SERVICE (Eureka) is only used when AUTH_SERVICE_URL is
        # empty AND a reachable Eureka registry provides instances.
        - id: auth-service
          uri: ${AUTH_SERVICE_URL:https://git-insight-auth.onrender.com}
          predicates:
            - Path=/api/auth/**
          metadata:
            response-timeout: 60000
            connect-timeout: 5000

        # ── Recruiter Service (auth-service) ──────────────────────────
        - id: recruiter-service
          uri: ${AUTH_SERVICE_URL:https://git-insight-auth.onrender.com}
          predicates:
            - Path=/api/recruiter/**
          metadata:
            response-timeout: 60000
            connect-timeout: 5000

        # ── Admin Service (auth-service) ──────────────────────────────
        - id: admin-service
          uri: ${AUTH_SERVICE_URL:https://git-insight-auth.onrender.com}
          predicates:
            - Path=/api/admin/**
          metadata:
            response-timeout: 60000
            connect-timeout: 5000

        # ── GitHub Service ────────────────────────────────────────────
        - id: github-service
          uri: ${GITHUB_SERVICE_URL:https://gitinsight-github-service.onrender.com}
          predicates:
            - Path=/api/github/**
          metadata:
            response-timeout: 120000
            connect-timeout: 5000

        # ── AI Service (github-service) ──────────────────────────────
        - id: ai-service
          uri: ${GITHUB_SERVICE_URL:https://gitinsight-github-service.onrender.com}
          predicates:
            - Path=/api/ai/**
          metadata:
            response-timeout: 120000
            connect-timeout: 5000

        # ── Reports Service (github-service) ─────────────────────────
        - id: reports-service
          uri: ${GITHUB_SERVICE_URL:https://gitinsight-github-service.onrender.com}
          predicates:
            - Path=/api/reports/**
          metadata:
            response-timeout: 120000
            connect-timeout: 5000

        # ── Analytics Service ─────────────────────────────────────────
        - id: analytics-service
          uri: ${ANALYTICS_SERVICE_URL:https://git-insight-ai.onrender.com}
          predicates:
            - Path=/api/analytics/**
          metadata:
            response-timeout: 60000
            connect-timeout: 5000

      httpclient:
        connect-timeout: 5000
        response-timeout: 60s

eureka:
  client:
    # Direct-URL routing is the default (Render). Enable only when a reachable
    # Eureka registry exists AND services register with routable hostnames.
    enabled: ${EUREKA_CLIENT_ENABLED:false}
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka/}
  instance:
    prefer-ip-address: true

server:
  port: ${PORT:8080}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: when_authorized

app:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:5174}
  jwt:
    secret: ${JWT_SECRET}
    min-secret-bytes: ${JWT_MIN_SECRET_BYTES:32}

logging:
  level:
    org.springframework.cloud.gateway: INFO
    com.gitinsight.gateway: INFO